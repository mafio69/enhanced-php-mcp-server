# WARNING: This file is tracked in Git.
# Put here ONLY non-sensitive, public configuration values.
# Secrets, passwords, API keys and tokens belong in .env (gitignored).
# docker compose picks up this file automatically as --env-file.

PORT=8795
